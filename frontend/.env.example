# VerdictGraph deployed addresses on GenLayer Testnet Bradbury.
# Do not fill these with old deployments: the submission integrity gate must tie
# them to the exact repository/deployment manifest before public release.
NEXT_PUBLIC_VERDICTGRAPH_CORE_ADDRESS=
NEXT_PUBLIC_VERDICTGRAPH_VAULT_ADDRESS=

# Network facts are intentionally hard-pinned in frontend/lib/genlayer/client.ts
# from the verified GenLayer Bradbury configuration (chain ID 4221).
